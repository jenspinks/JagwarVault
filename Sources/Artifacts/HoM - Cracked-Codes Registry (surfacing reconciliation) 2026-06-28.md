---
type: artifact
citable: true
status: pointer
captured: 2026-06-28
purpose: "Audit record of the 2026-06-28 surfacing reconciliation. The CANONICAL live status list is _System/HoM - Finds Ledger — maintain that one, not this."
---
# HoM — Cracked-Codes audit (2026-06-28) → see the canonical ledger

**▶ Canonical living status list: [[_System/HoM - Finds Ledger (all cracks + status)]].** This doc is just the audit that fed it — don't maintain two ledgers (two competing lists *was* the fragmentation that let finds slip).

## What the audit did (the guarantee, reproducible)
Concern: codes were cracked by automated sweeps, filed to CSVs, and **never surfaced to Jen** (e.g. `a bullet in her genes`, `the way to Heaven` — found, buried, only re-surfaced when re-found). To guarantee none are still hidden:

1. **Exhaustive vault sweep** — gathered **every string in every vault file** (437 files incl. the compressed 716k-candidate set) = **778,299 distinct strings → 4,041,716 variant-hashes** → tested against the 42 open Merkle leaves (`keccak256(lower) ∈ open-set`; harness in `Data/merkle-coverage-2026-06-28/`).
2. **Result: exactly 7 open-leaf hits, the same 7** — `in my father's shoes`, `no thing creates another thing`, `a bullet in her genes`, `the way to Heaven` (openable) + `there are things behind things`, `genesis tree code`, `the great jagwar` (already-minted gap-fills). **No other discovered/openable code exists anywhere in the vault.**
3. **Jen confirmed (2026-06-28)** she has opened/minted **all** finds — no unclaimed backlog. STARDUST = already-minted numbered room.
4. Re-pulled all frontier artwork to `Media/images/hom-artifacts/reconciled-2026-06-28/` so nothing is lost to bucket decay.

**Conclusion:** the gap was *recording-only*, now closed. Every cracked code is claimed and logged in the canonical ledger.

## Standing rule (also stamped on the ledger)
Every new crack → (a) add to [[_System/HoM - Finds Ledger (all cracks + status)]] with status, AND (b) tell Jen in chat as "code → what it opens → openable or claimed." Never just file to a data file.
